# FastAPI
fastapi[standard]==0.115.*
uvicorn[standard]==0.34.*
pydantic-settings==2.*

# Database
sqlalchemy[asyncio]==2.*
asyncpg==0.30.*
alembic==1.14.*

# Redis
redis[hiredis]==5.*

# HTTP client (Wazuh API calls)
httpx==0.28.*

# Auth
passlib==1.7.*
bcrypt>=3.2,<4.0
python-jose[cryptography]==3.3.*

# Utilities
python-dateutil==2.*

# Module 2: Enrichment worker (uses httpx already listed above)
# No additional deps required — httpx + redis already present

# Module 3: Playbook engine
pyyaml==6.*

# Evidence storage (MinIO S3 client). sync API wrapped in asyncio.to_thread —
# MinIO doesn't ship an async client and a sync wrap is lighter than aioboto3.
minio==7.*
